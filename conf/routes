# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET         /                    controllers.HomeController.index
GET         /explore             controllers.HomeController.explore
GET         /tutorial            controllers.HomeController.tutorial

GET         /hello               controllers.UserController.hello(name: String)

GET         /list                controllers.UserController.list()
GET         /user                controllers.UserController.user(id: Long)

+ nocsrf
POST        /user                controllers.UserController.newUser(request: Request)


GET         /auth                controllers.UserController.authenticatedCachedIndex()



# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.versioned(path="/public", file: Asset)
